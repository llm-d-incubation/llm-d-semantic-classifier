# Generated by round2/gen_routes.py -- 4-cluster route table.
#
# Every tier points at the SAME backend on purpose. The variable under test is
# the size of the route table the gateway must evaluate, so the destination must
# be held constant; otherwise a routing change would also change the backend and
# the two effects would be inseparable.
admin:
  address: "0.0.0.0:9901"
  verbose: true
insecure_options:
  allow_public_admin: true
listeners:
  - name: http
    address: "0.0.0.0:8080"
    filter_chains: [ai-routing]
  - name: baseline
    address: "0.0.0.0:8081"
    filter_chains: [baseline]
filter_chains:
  - name: ai-routing
    filters:
      - filter: request_id
      - filter: access_log
      - filter: llm_d_sc
        endpoint: "llm-d-sc.cnuland-dev.svc.cluster.local:50051"
        default_cluster: general
        timeout_ms: 1000
        routes:
          - {label: TIER00, cluster: tier00}
          - {label: TIER01, cluster: tier01}
          - {label: TIER02, cluster: tier02}
          - {label: TIER03, cluster: tier03}
      - filter: load_balancer
        clusters:
          - name: tier00
            endpoints: ["vcr-small.cnuland-dev.svc.cluster.local:8000"]
          - name: tier01
            endpoints: ["vcr-small.cnuland-dev.svc.cluster.local:8000"]
          - name: tier02
            endpoints: ["vcr-small.cnuland-dev.svc.cluster.local:8000"]
          - name: tier03
            endpoints: ["vcr-small.cnuland-dev.svc.cluster.local:8000"]
          - name: general
            endpoints: ["vcr-small.cnuland-dev.svc.cluster.local:8000"]
  - name: baseline
    filters:
      - filter: request_id
      - filter: access_log
      - filter: router
        routes:
          - path_prefix: "/"
            cluster: general
      - filter: load_balancer
        clusters:
          - name: general
            endpoints: ["vcr-small.cnuland-dev.svc.cluster.local:8000"]
