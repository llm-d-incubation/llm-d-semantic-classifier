# 0.20 Phase Specification

Bounded scheduler, deadlines/cancellation, load shedding, readiness/liveness, graceful drain, structured errors, metrics and prompt-redaction hardening.

Before implementation, expand this file into the standard SDD fields and map every acceptance criterion to `tests/TEST_MATRIX.md`.
