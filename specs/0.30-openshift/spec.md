# 0.30 Phase Specification

Private registry/digest pinning, disconnected start, random UID, NetworkPolicy, scaling, rolling revisions, disruption, resource pressure and observability.

Before implementation, expand this file into the standard SDD fields and map every acceptance criterion to `tests/TEST_MATRIX.md`.
