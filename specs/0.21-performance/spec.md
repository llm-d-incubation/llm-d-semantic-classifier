# 0.21 Phase Specification

Repeatable CPU/GPU/cache/input-length/concurrency/topology characterization and named hardware profiles; propose hard SLO only after evidence.

Before implementation, expand this file into the standard SDD fields and map every acceptance criterion to `tests/TEST_MATRIX.md`.
