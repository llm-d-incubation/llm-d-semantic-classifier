# 0.24 Phase Specification

Backend conformance, server-independent runtime-core, candidate load/warm/atomic swap/drain/unload and alternative backend seam.

Before implementation, expand this file into the standard SDD fields and map every acceptance criterion to `tests/TEST_MATRIX.md`.
