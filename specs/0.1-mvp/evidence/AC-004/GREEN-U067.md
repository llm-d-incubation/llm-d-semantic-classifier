# AC-004 GREEN evidence — U-067

## Proving test
- U-067 `u067_golden_fixture_ranking_matches_reference` (`src/embedding.rs`,
  `#[ignore]`d, run with `-- --ignored` after `./hack/fetch-model`).
- Loads the resident embedder, embeds the golden input
  `"this is a golden sensitivity input"`, cosine-ranks the committed SYNTHETIC
  prototypes (`tests/fixtures/modelcar/synthetic-prototypes.json`), and asserts
  (a) the rank order matches the golden-ranking fixture exactly and (b) each
  score is within `1e-4` of the fixture.

## Golden fixture (new)
`tests/fixtures/modelcar/golden-ranking.json` — generated by
`artifacts/gen_golden_ranking.py` (python3) using the pinned
sentence-transformers stack:
```
sentence_transformers.SentenceTransformer("artifacts/models/sensitivity")
  .encode("this is a golden sensitivity input")
```
then cosine-ranked against the 4 SYNTHETIC prototypes. Provenance is embedded in
the fixture (stack, input, `prototypes: SYNTHETIC for mechanics only ... NOT a
real taxonomy`, generator script, date). The reference ranking order is
`[proto-a, proto-d, proto-c, proto-b]` with scores matching the embedding's
first-four dims.

## Smallest change
- New fixture `tests/fixtures/modelcar/golden-ranking.json` (golden pipeline
  fixture).
- `src/embedding.rs`: added `u067_golden_fixture_ranking_matches_reference`
  (`#[ignore]`) plus a `synthetic_prototypes()` helper. Composes existing
  `Embedder::embed` + `crate::ranker::cosine_rank`.

## Command
```
./hack/fetch-model
cargo test --locked -- --ignored u067_golden_fixture_ranking_matches_reference
```

## Result
```
running 1 test
test embedding::tests::u067_golden_fixture_ranking_matches_reference ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 15 filtered out
```

## Worktree / SHA
- SHA: `7736ea9` (uncommitted)
- No commits/pushes.
