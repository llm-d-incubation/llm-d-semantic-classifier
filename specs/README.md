# Specs

One directory per substantial change. Copy `TEMPLATE/` to `specs/<id>/` (e.g. `specs/001-grpc-skeleton/`).
The spec is the first implementation artifact: no implementation turn runs without an approved spec.
The maintainer will provide the SDD/TDD files for the initial service milestones.
